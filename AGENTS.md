# AGENTS.md

## 프로젝트 개요
- 이 프로젝트는 움직임이 불편한 노인과 장애인을 위한 헬스케어 모션 게임을 Unity 환경에서 개발하는 것을 목표로 한다.
- 초기 기획서에는 웹 게임과 Unity 연동이 함께 포함되어 있었으나, 현재 구현 기준은 Unity 단일 환경이다.
- 플레이는 앉은 자세를 기본으로 하며, 상체와 팔 중심의 안전한 동작을 게임 입력으로 사용한다.

## 핵심 개발 원칙
- 모든 구현은 접근성, 안전성, 반복 사용성을 우선한다.
- 과한 액션성보다 안정적인 인식과 명확한 피드백을 우선한다.
- 게임 판정은 사용자의 신체 조건 차이를 고려해 관대하게 설계한다.
- 카메라 인식 실패가 즉시 사용자 실패로 이어지지 않도록 보정 흐름을 둔다.

## Unity 기본 지침

### 카메라
- 메인 카메라 Projection은 반드시 `Orthographic`을 사용한다.
- 카메라 세팅은 UI, 캐릭터, 배경이 1920 x 1080 기준 화면에서 안정적으로 보이도록 맞춘다.
- 게임 플레이 핵심 요소는 안전한 시야 범위 안에 배치한다.

### 해상도
- 기본 작업 해상도는 반드시 `1920 x 1080`으로 한다.
- UI 레이아웃, 연출, 판정 영역은 Full HD 기준으로 먼저 설계한다.
- 해상도 확장이 필요할 경우에도 1920 x 1080을 기준 해상도로 유지한다.

### 플랫폼/구조
- 현재 범위에서는 웹 게임 구현을 제외하고 Unity 단일 실행 환경으로 개발한다.
- 입력은 MediaPipe 기반 외부 인식 결과 또는 Unity 내부 테스트 입력으로 수용할 수 있게 설계한다.
- 실제 인식 연결 전에도 테스트 가능한 입력 시뮬레이션 구조를 유지한다.

## 입력 및 판정 지침
- 입력은 상체 좌우 이동, 양팔 올리기, 한쪽 팔 뻗기 등 좌식 환경에 적합한 동작부터 시작한다.
- 판정 로직은 원시 좌표 처리와 게임 판정 처리를 분리한다.
- 흔들림을 줄이기 위해 보간, 임계값, 유지 시간 기준을 둔다.
- 실패 판정은 공격적으로 하지 말고 재시도 기회를 충분히 제공한다.

## UX/UI 지침
- 큰 글씨, 높은 대비, 단순한 인터페이스를 기본으로 한다.
- 성공/실패/대기 상태를 색상 외에도 아이콘, 텍스트, 애니메이션으로 함께 전달한다.
- 한 화면에 너무 많은 정보나 조작 요소를 배치하지 않는다.
- 튜토리얼과 자세 맞춤 화면은 실제 게임보다 더 단순하고 친절해야 한다.

## 안전 및 접근성 지침
- 서 있는 자세를 요구하지 않는다.
- 과도한 어깨 가동, 허리 비틀기, 빠른 반복 동작을 기본 요구사항으로 두지 않는다.
- 통증, 피로, 인식 오류가 누적될 때 즉시 중단 또는 휴식 안내가 가능해야 한다.
- 보호자나 운영자가 개입하기 쉬운 UI 흐름을 고려한다.

## 씬 및 코드 구조 지침
- 씬은 역할별로 분리한다.
- 최소 권장 씬 구성: `Boot`, `Title`, `Calibration`, `Gameplay`, `Result`
- 게임 규칙, 입력 처리, UI 상태, 점수 계산은 서로 분리된 컴포넌트로 관리한다.
- 특정 콘텐츠 로직을 공용 입력 처리 계층에 강하게 결합하지 않는다.

## 네이밍 지침
- 폴더와 파일 이름은 가급적 영어를 사용한다.
- 사용자 문서와 기획 문서는 한국어 파일명을 허용한다.
- 스크립트 이름은 역할이 드러나도록 `MotionInputReceiver`, `CalibrationManager`, `GameSessionController`처럼 명확하게 작성한다.

## 테스트 지침
- 실제 카메라/인식 연결 전에도 키보드 또는 더미 데이터로 플레이 테스트가 가능해야 한다.
- 각 단계 완료 시 Calibration, Gameplay, Result 흐름을 최소 1회 이상 수동 검증한다.
- 판정 튜닝 시 고정된 테스트 시나리오를 유지해 회귀를 확인한다.

## 문서화 지침
- 기능 추가 시 `Plan` 폴더의 해당 체크리스트를 함께 갱신한다.
- 새로운 입력 방식, 판정 기준, UI 흐름이 생기면 관련 문서를 먼저 업데이트한다.
- 구현 우선순위가 바뀌면 작업 순서 문서와 일정 문서를 함께 수정한다.

## MVP 우선순위
- 1차 목표는 Unity 내에서 앉은 자세 기반 모션 게임 1종을 완성하는 것이다.
- 필수 범위는 Calibration, 기본 동작 3종 판정, 점수 반영, 결과 화면, 테스트 입력 지원이다.
- Unity와 외부 인식 시스템의 실연동은 MVP 후반부 또는 병행 검증 단계에서 다룬다.
